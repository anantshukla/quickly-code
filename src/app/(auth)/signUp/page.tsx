import Image from "next/image";

const page = () => {
	return (
		<main className="flex min-h-screen flex-col items-center justify-between p-24">
			<p className="m-0 max-w-[30ch] text-sm opacity-50">SignUp</p>
		</main>
	);
};

export default page;
